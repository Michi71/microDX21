Use case: infographic-diagram, LEGO-instruction style exploded isometric view.
Primary request: Exploded isometric "LEGO-style" assembly diagram of a microDX21 synth stack for a Raspberry Pi Zero 2 WH. Vertical stack, top to bottom, with clear vertical alignment and small motion-guide arrows connecting each layer. Show the KY-040 rotary encoder as a separate, loose component OUTSIDE the stack (off to the side), connected only by a 5-wire ribbon cable — it is NOT mounted on the proto bonnet.
Scene/backdrop: clean white technical-drawing background with a faint neutral grid; flat colors, no photorealism, no gradients on parts.
Stack from TOP to BOTTOM, each layer a separate flat rectangular board drawn in slight isometric perspective and labelled on the right:
1. TOP LAYER - "Waveshare 2.23 inch OLED HAT (SSD1305, 128x32, SPI/I2C)". Show the small rectangular OLED display module with the visible 128x32 pixel screen on the top, a thin blue PCB below it, and a 2x20 GPIO female socket on the underside. Label: "Display: 128x32 OLED (SSD1305/SH1106)".
2. SECOND LAYER - "Adafruit Perma-Proto Bonnet Mini (ADA3203)". A small green prototyping PCB with two rows of pin holes, a few labeled through-hole pads (e.g. "GND", "3V3", "GPIO17/ENC_A", "GPIO27/ENC_BTN", "I2S BCLK/LRC/DATA"), and a 2x20 GPIO female header on top and a 2x20 male pin header on the bottom. On this board the ONLY mounted components are wiring-related: a 5-pin header or 5 solder pads labelled in a row "ENC_A", "ENC_B", "ENC_SW", "GND", "+3V3" where a 5-wire ribbon cable terminates; the board has NO KY-040 encoder, NO 3.5 mm jack, NO line-out, NO headphone jack. Label: "Proto Bonnet: pass-through + 5-wire encoder breakout (no jacks, no encoder)".
3. THIRD LAYER - "WM8960 Hi-Fi Sound Card HAT (I2S/I2C)". A rectangular red PCB with a centered WM8960 QFN chip outline, two 3.5 mm line-out/headphone jacks on the front edge labeled "L" and "R" (these are the only audio outputs of the whole system — there is NO jack on the proto bonnet), a small speaker-output terminal block labeled "SPK+/SPK-", a pin header labeled "I2S / I2C", and a 2x20 GPIO female header on top and male header on the bottom. Label: "DAC: WM8960 (I2S, headphone + line out)".
4. BOTTOM LAYER - "Raspberry Pi Zero 2 WH". The Pi board in its standard layout: small green PCB, micro-USB power on the left edge, micro-HDMI on the left edge, microSD slot on the underside (visible as a cutout), a 2x20 male GPIO header rising from the top of the board, the BCM2710A1 SoC square, the metallic shielding can, and the "Raspberry Pi Zero 2 WH" silkscreen text. Label: "Raspberry Pi Zero 2 WH (BCM2710A1, 1 GHz, 512 MB)".
Off the stack (outside, drawn to the RIGHT of the layers, NOT between any two boards): show a small loose KY-040 rotary encoder with its D-shaft visible, labelled "KY-040 (loose, off-board)". From the KY-040 draw a clearly visible 5-wire ribbon cable that routes back to the proto bonnet and terminates at the 5-pin header "ENC_A / ENC_B / ENC_SW / GND / +3V3" on the proto bonnet. The cable should be a single flat ribbon drawn as 5 parallel lines, with each wire labelled at both ends ("ENC_A", "ENC_B", "ENC_SW", "GND", "+3V3"). The encoder must visually be on its own, with a small "not on the board" note.
On the LEFT side of the diagram, draw four short curved arrows pointing downward labeled "1", "2", "3", "4" indicating the assembly order top-to-bottom for the four stacked boards. On the RIGHT side, draw four short vertical alignment arrows showing the GPIO 2x20 pin headers passing through all four boards (the loose encoder sits to the right of the stack and does NOT plug into the GPIO headers).
Style/medium: clean flat vector-style isometric technical illustration, like a LEGO building instruction step, with crisp black outlines, flat color fills (Pi green, OLED black, proto green, WM8960 red, encoder grey), subtle drop shadow under each board, generous spacing between exploded layers, and no photorealism.
Composition/framing: portrait A3/tabloid-style layout, 4:5 ratio, the stack centered horizontally, all four boards fully visible and not overlapping, the loose KY-040 + ribbon cable clearly visible to the right of the stack with enough space to draw the 5-wire cable routing, labels readable, generous margins.
Text (verbatim, in this exact order on the right side of each board, in a clean sans-serif):
"1. OLED HAT - SSD1305 128x32 (SPI/I2C)"
"2. Proto Bonnet - 5-wire encoder breakout (no jacks, no encoder)"
"3. WM8960 HAT - I2S DAC, headphone + line out"
"4. Raspberry Pi Zero 2 WH - BCM2710A1, 512 MB"
And on the loose encoder callout (right side, lower half):
"KY-040 (loose) - 5-wire ribbon: ENC_A, ENC_B, ENC_SW, GND, +3V3"
Constraints: no logos or trademarks; no watermark; no photorealism; clean line work; readable labels; no extra text besides the labels above, the step numbers 1-4, the GPIO-stack annotation, and the wire labels "ENC_A / ENC_B / ENC_SW / GND / +3V3"; use the exact wording above; the proto bonnet must have NO 3.5 mm jack and NO KY-040 mounted on it; the KY-040 must be shown as a loose part outside the stack; the WM8960 board is the only board carrying audio output jacks.
Avoid: gradients, photoreal textures, drop shadows on text, mirrored or upside-down boards, overlapping boards, hand-drawn sketch style, mounting the KY-040 on any of the four stacked boards, putting a jack on the proto bonnet.
