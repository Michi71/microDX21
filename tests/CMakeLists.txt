# Unit tests for the microDX21 synth engine. Tests are added to the build
# by default (CMake's `enable_testing()` is called in the top-level
# CMakeLists.txt). Each test is a standalone executable that exits 0
# on success, non-zero on failure.
#
# Add new tests here as you write them; they'll be picked up automatically
# by the next cmake configure.
